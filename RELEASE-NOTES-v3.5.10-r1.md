# RescueX v3.5.10-r1

> 同级修复版：完整修复启动统计计数（配对计数），版本号采用
> `v3.5.10-r1` / versionCode `350201`（主版本不跳级，后缀递增）。

## 修复：启动统计成功率虚高（治本）

**问题**：此前 `compute_boot_stats` 把"所有 SERVICE 行数"当作成功次数，
不校验该行是否有对应的 START 启动记录。历史 `boot_history` 中若存在
孤儿 SERVICE 行（无对应 START 的脏数据/重复行），成功次数会超过总启动次数，
此前版本的简单钳制（SUCCESS=TOTAL）会让成功率永远显示 100%——
即使实际发生了救砖/失败启动，也会被虚高的成功计数掩盖。

**修复**：改为**配对计数**：

- `SUCCESS` 只统计「boot token 能在 START 行中找到」的 SERVICE 行；
- 同一 token 的重复 SERVICE 行只计一次；
- 孤儿 SERVICE 行（无对应 START）不再计入成功；
- 老格式（无 `boot=` 字段）行保持旧行为兼容。

效果：救砖/失败的启动（只有 START 无 SERVICE）会正确拉低成功率；
历史脏数据不再虚高。`SUCCESS ≤ TOTAL` 数学上必然成立，
原钳制逻辑保留作为最终防线。

离线验证（5 场景全通过）：

| 场景 | 结果 |
|---|---|
| 正常 4 次全成功 | 4/4 = 100% |
| 脏数据 3 条孤儿 SERVICE | 4/4 = 100%（孤儿不计） |
| 含 1 次救砖（5 START / 4 SERVICE） | 4/5 = 80% |
| 重复 SERVICE 行（同 token） | 只计一次，1/1 = 100% |
| 老格式无 boot 字段 | 保持旧行为 |

## 版本号规则（从本版起）

- 主版本：`v3.5.10` = versionCode `35020`
- 同级修复：`v3.5.10-rN` = versionCode `35020` 追加两位：`350201`、`350202`…
- 不再通过跳主版本号来发小修复，避免 versionCode 快速涨级。

## 继承

v3.5.10 全部功能（音量键 WebUI 选择、无 WebUI 模式、完整性动态清单）与
v3.5.9-r1/r2 全部修复均保留。

## 验证

- POSIX shell 语法检查全部通过（sh -n）。
- JavaScript 语法检查通过。
- 配对计数 5 场景离线测试通过。
