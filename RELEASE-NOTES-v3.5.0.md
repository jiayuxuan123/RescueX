# RescueX v3.5.0

> 正式发布构建。

## 本次更新

### 救援可观测性
- 新增救援时间线、服务健康状态、模块清单基线与最近变更识别。
- 状态和模拟功能均只读，不会修改第三方模块。

### 快照与恢复
- 支持快照命名、固定、差异查看和选择性恢复。
- 固定快照不会被自动清理；选择性恢复只修改用户选中的模块。

### 一次性安全模式
- 新增“仅下一次启动安全模式”。
- 先原子固化本次事务的精确恢复清单，再写入本次涉及模块的禁用标记。
- 成功启动或用户取消时，只恢复该清单里由 RescueX 写入的标记。
- 不触碰白名单模块、第三方预先禁用模块或第三方脚本。

### 脱敏诊断包
- 新增默认脱敏的 ZIP 诊断包，包含状态、时间线、模块清单和有限日志摘要。
- 归档工具优先使用系统 `zip`，并兼容 Magisk、KernelSU、APatch 的常见 BusyBox 路径。

### WebUI
- 新增受控入口：只读救援模拟、安全模式状态、一次性布防/精确取消、脱敏诊断导出。
- WebUI 不接收任意 Shell 输入；安全模式操作均有确认提示。

## 回归验证

- v3.5 专项隔离测试：模块变更、固定快照、选择性恢复、一次性安全模式精确所有权、只读模拟、诊断脱敏。
- 原有快照回归与 8 项安全回归。
- Shell、JavaScript、HTML、发布合同与安装 ZIP 结构/权限检查。

## 发布信息

- 版本：`v3.5.0` (`versionCode 35000`)
- 安装包：`RescueX-v3.5.0.zip`
